# ── 敏感配置（真实凭证、文档 ID、项目代号）──
config/config.json
config/publish.json
config/games.json
config/scoring.json
config/derived/
config/local_backup/

# ── 运行时数据 ──
data/

# ── Python 缓存 ──
__pycache__/
*.pyc
*.pyo

# ── 本地状态 ──
.mcporter/
